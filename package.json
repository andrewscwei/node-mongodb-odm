{
  "name": "mongodb-odm",
  "version": "0.1.0",
  "description": "ODM for MongoDB",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf build",
    "build": "npm run clean && tsc",
    "test": "npm run build && NODE_ENV=test DEBUG=mongodb-odm* mocha --require dotenv/config --timeout 10000 --reporter spec --full-trace --colors --recursive --bail --exit $([ -z \"$npm_config_path\" ] && echo build || echo $npm_config_path)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/sybl/mongodb-odm.git"
  },
  "keywords": [
    "mongodb",
    "odm"
  ],
  "author": "Andrew Wei <andrewscwei@gmail.com>",
  "license": "UNLICENSED",
  "bugs": {
    "url": "https://github.com/sybl/mongodb-odm/issues"
  },
  "homepage": "https://github.com/sybl/mongodb-odm#readme",
  "files": [
    "build"
  ],
  "dependencies": {
    "@sindresorhus/is": "^0.12.0",
    "bcrypt": "^3.0.2",
    "debug": "^4.1.0",
    "faker": "^4.1.0",
    "mongodb": "^3.1.8"
  },
  "devDependencies": {
    "@types/bcrypt": "^3.0.0",
    "@types/debug": "^0.0.31",
    "@types/dotenv": "^4.0.3",
    "@types/faker": "^4.1.4",
    "@types/lodash": "^4.14.117",
    "@types/mocha": "^5.2.5",
    "@types/mongodb": "^3.1.12",
    "dotenv": "^6.1.0",
    "lodash": "^4.17.11",
    "mocha": "^5.2.0",
    "rimraf": "^2.6.2",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typescript": "^3.1.3"
  },
  "types": "build/index.d.ts"
}
